\begin{tabular}{lcc}
\toprule
Warmth level & Main MNL $\Delta_{pp}$(S+A) & Nested logit $\Delta_{pp}$(S+A) \\
\midrule
$-1$ SD & 0.2985 & NE \\
$0$ SD & 0.3068 & NE \\
$+1$ SD & 0.3113 & NE \\
\bottomrule
\end{tabular}
% Note: Nested logit converges (logLik=-4601.8, unweighted) with nests {Neither, S+A} / {Sanctions-only, Action-only}, but inclusive-value parameters lambda_1=0.73, lambda_2=-2.62 fall outside the [0,1] range required for a proper nested-logit interpretation. The IIA rejection therefore does not admit a clean nested-logit representation under this nesting; we report the main MNL Delta_pp(S+A) as the benchmark. NE = not estimated due to bound violation.
